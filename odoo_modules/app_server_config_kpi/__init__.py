# No Python. This module is a view + an OWL component: it adds the Users screen
# to app_server_config's form and menu, and writes through kra_kpi_module's
# existing /kpi_user_access/* routes. It defines no models and no data of its
# own — that is what keeps this a duplicated UI rather than a duplicated source
# of truth.
