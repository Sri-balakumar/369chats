<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ✅ Server Action to Open Password Config (Singleton) -->
    <record id="action_open_password_config" model="ir.actions.server">
        <field name="name">Open Password Config</field>
        <field name="model_id" ref="model_kpi_cleanup_password_config"/>
        <field name="state">code</field>
        <field name="code">
# Get or create the singleton config record
config = env['kpi.cleanup.password.config'].sudo().get_or_create_config()

# Return action to open the form view
action = {
    'type': 'ir.actions.act_window',
    'name': 'Data Cleanup Password',
    'res_model': 'kpi.cleanup.password.config',
    'view_mode': 'form',
    'res_id': config.id,
    'target': 'current',
    'context': {'form_view_initial_mode': 'edit'},
}
        </field>
    </record>

    <!-- ✅ Updated Menu to use Server Action -->
    <menuitem id="menu_kpi_cleanup_password_updated"
              name="Data Cleanup Password"
              parent="menu_kpi_settings"
              action="action_open_password_config"
              sequence="10"
              groups="base.group_system"/>
</odoo>